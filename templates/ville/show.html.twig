{% extends 'base.html.twig' %}

{% block title %}Hello VilleController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container">
    
    <div class="row">
        <div class="col">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="{{ville.image}}" alt="Dét" width="500">
                <div class="card-body">
                    <h5 class="card-title">{{ville.name}}</h5>
                    <p class="card-text"> CP {{ville.postalCode}}</p>
                    <p class="card-text"> Population: {{ville.population}}</p>
                    <p class="card-text"> Département: {{departementVille}}</p>
                </div>

                {# {% if is_granted ('ROLE_STAFF') %}
                <a class="btn btn-info" href="{{ path('admin_editProduct', {id:ville.id,slug:ville.slug}) }}">Editer</a>  
                {% endif %}

                {% if is_granted ('ROLE_STAFF') %}
                <a class="btn btn-info" href="{{ path('admin_deleteProduct', {id:ville.id}) }}"> Supprimer </a>  
                {% endif %} #}

            </div>
        </div>
    </div>
</div>
{% endblock %}
